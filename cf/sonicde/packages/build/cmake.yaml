sources:
    git:
        url:    https://gitlab.kitware.com/cmake/cmake.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref: v3.28.0

buildsystem: cmake

cmake-extra-args:
    - "-G"
    - Ninja
