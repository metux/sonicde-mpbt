sources:
    git:
        url:    ${sonicde_git}/${@basename}
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/heads/*:refs/remotes/origin/*

    git-ext:
        upstream:
            url: ${kf6_git}/kglobalaccel.git
            fetch:
              - refs/heads/*:refs/remotes/upstream/*
              - refs/tags/*:refs/tags/upstream/*

ref: origin/master

buildsystem: cmake

build-depends:
    - kde/kf6/extra-cmake-modules
