provides: kde/kf6/krunner
sources:
    git:
        url:    ${@SOLUTION::sonicde_git}/${@basename}
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/*:refs/tags/*
          - refs/heads/*:refs/remotes/origin/*

# ref: ${@SOLUTION::kf6_version}
ref: origin/master

buildsystem: cmake

depends:
    - build/cmake
    - qt/qt6-base
    - kde/extra-cmake-modules
    - kde/kconfig
    - kde/breeze-icons
    - kde/kwindowsystem
    - kde/kitemmodels
