sources:
    git:
        url:    ${@SOLUTION::sonicde_git}/${@basename}
        ref:    ${ref}
        fetch:
          - refs/heads/*:refs/remotes/origin/*
          - refs/tags/*:refs/tags/*

ref: origin/master

buildsystem: cmake

depends:
    - qt/qt6-declarative
    - qt/qt6-svg

build-depends:
    - buildtool/extra-cmake-modules
