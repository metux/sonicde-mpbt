sources:
    git:
        url:    ${@SOLUTION::sonicde_git}/${@basename}
        ref:    ${ref}
        fetch:
          - refs/tags/*:refs/tags/*
          - refs/heads/*:refs/remotes/origin/*

# ref: ${@SOLUTION::kf6_version}
# ref: origin/master
ref: origin/mtx/wip

buildsystem: cmake

build-depends:
    - kde/kf6/extra-cmake-modules

depends:
    - qt6/qtbase
    - kde/kf6/kconfig
    - kde/kf6/breeze-icons
    - kde/kwindowsystem
    - kde/kf6/kitemmodels
