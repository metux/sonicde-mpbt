sources:
    git:
        url:    https://code.qt.io/qt/${@basename}.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref: ${@SOLUTION::qt6_version}

buildsystem: cmake

depends:
    - qt6/qtbase

cmake-extra-args:
    - -DFEATURE_developer_build=ON
