sources:
    git:
        url:    git://code.qt.io/qt/qt5.git
        ref:    ${ref}
        fetch:
          - refs/heads/*:refs/remotes/origin/*
          - refs/tags/*:refs/tags/*
        post-checkout-cmd: ${source-dir}/init-repository

ref: v6.10.2

buildsystem: cmake
