sources:
    git:
        url:    https://code.qt.io/qt/qtbase.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}*:refs/tags/${ref}

ref: v6.10.2

buildsystem: cmake

cmake-extra-args:
    - "-G"
    - Ninja
