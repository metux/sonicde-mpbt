sources:
    git:
        url:    ${kdelibs_git}/${@basename}.git
        ref:    ${ref}
        depth:  1
        fetch:
          - +refs/heads/*:refs/remotes/origin/*
        force-checkout: ${kdelibs_force}

ref: origin/master

buildsystem: cmake

depends:
    - qt6/qtbase
