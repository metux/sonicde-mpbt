sources:
    git:
        url:    https://invent.kde.org/plasma/plasma5support.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref: ${@SOLUTION::plasma_version}

buildsystem: cmake

build-depends:
    - build/cmake
    - kde/extra-cmake-modules

depends:
    - qt6/qtbase
    - kde/kidletime
    - kde/kunitconversion
    - kde/kholidays
