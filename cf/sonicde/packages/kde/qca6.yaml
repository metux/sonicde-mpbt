sources:
    git:
        url:    https://github.com/KDE/qca
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref: v2.3.10

buildsystem: cmake

depends:
    - build/cmake
    - kde/extra-cmake-modules
    - qt/qt6-base

cmake-extra-args:
    - -GNinja
    - -DBUILD_WITH_QT6=ON
