provides: buildtool/extra-cmake-modules
sources:
    git:
        url:    ${@SOLUTION::kde_git}/${@basename}
        ref:    ${ref}
        fetch:
          - refs/heads/*:refs/remotes/origin/*
          - refs/tags/*:refs/tags/*

ref: v6.23.0

buildsystem: cmake

build-depends:
    - build/cmake
    - qt/qt6-base
