provides: buildtool/extra-cmake-modules
sources:
    git:
        url:    ${@SOLUTION::kde_git}/${@basename}
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref: v6.23.0

buildsystem: cmake

build-depends:
    - build/cmake
    - qt/qt6-base
