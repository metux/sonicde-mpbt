sources:
    git:
        url:    ${@SOLUTION::kde_git}/${@basename}
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref: ${@SOLUTION::kde_version}

buildsystem: cmake

depends:
    - platform/libcanberra
    - build/cmake
    - kde/extra-cmake-modules
    - qt/qt6-base
