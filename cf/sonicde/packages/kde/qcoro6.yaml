sources:
    git:
        url:    https://github.com/danvratil/qcoro.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref:  v0.13.0

buildsystem: cmake

build-depends:
    - 3rdparty/cmake

depends:
    - qt6/qtbase
    - qt6/qtwebsockets
