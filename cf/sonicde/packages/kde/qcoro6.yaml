sources:
    git:
        url:    https://github.com/danvratil/qcoro.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref:  v0.13.0

buildsystem: cmake

depends:
    - build/cmake
    - qt/qt6-base
    - qt6/qtwebsockets
