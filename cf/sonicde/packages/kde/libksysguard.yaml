sources:
    git:
        url:    https://invent.kde.org/plasma/${@basename}.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref: ${@SOLUTION::plasma_version}

buildsystem: cmake

depends:
    - build/cmake
    - kde/extra-cmake-modules
    - qt/qt6-base
