sources:
    git:
        url:    ${kf6_git}/${@basename}
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref: ${kf6_version}

buildsystem: cmake

depends:
    - kde/kf6/extra-cmake-modules
