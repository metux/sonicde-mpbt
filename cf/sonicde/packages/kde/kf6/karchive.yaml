sources:
    git:
        url:    ${@SOLUTION::kf6_git}/${@basename}
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref: ${@SOLUTION::kf6_version}

buildsystem: cmake

build-depends:
    - qt6/qttools
    - kde/kf6/extra-cmake-modules

# debian: needs liblzma-dev
