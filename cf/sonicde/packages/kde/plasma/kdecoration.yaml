sources:
    git:
        url:    ${@SOLUTION::plasma_git}/${@basename}.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/${ref}:refs/tags/${ref}

ref: ${@SOLUTION::plasma_version}

buildsystem: cmake

depends:
    - build/cmake
    - qt/qt6-base
