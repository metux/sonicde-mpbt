sources:
    git:
        url:    ${@SOLUTION::kde_git}/${@basename}
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/heads/*:refs/remotes/origin/*
          - refs/tags/*:refs/tags/*

ref: v6.23.0

buildsystem: cmake

depends:
    - kde/karchive
    - kde/kconfig
    - kde/kcolorscheme
