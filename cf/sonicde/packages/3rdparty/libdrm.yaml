provides: sys/libdrm

sources:
    git:
        url:    ${xlibre_git}/mirror.fdo.drm.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/tags/*:refs/tags/*

buildsystem: meson

ref: libdrm-2.4.121
